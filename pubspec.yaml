name: reading_monster
description: App đọc sách EPUB và nuôi linh thú.
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Đã cập nhật phiên bản mới hơn để tránh lỗi build
  vocsy_epub_viewer: ^1.1.0 
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
